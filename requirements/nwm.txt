# NWM runtime dependencies. PyTorch is installed separately by setup_nwm_env.sh
# so CPU/CUDA wheel selection remains explicit.
accelerate>=0.34,<2
# The original decord 0.6.0 Linux wheel declares a CPython 3.6 ABI tag in
# its internal metadata. decord2 keeps the same `import decord` API and ships
# a native CPython 3.10 wheel.
decord2==3.4.0
diffusers>=0.30,<1
dreamsim>=0.2,<1
evo>=1.31,<2
ipython>=8,<10
lpips==0.1.4
matplotlib>=3.8,<4
numpy>=1.26,<2
pandas>=2.2,<3
Pillow>=10,<13
scikit-learn>=1.4,<2
scipy>=1.12,<2
submitit>=1.5,<2
torcheval==0.0.7
torchmetrics>=1.4,<2
tqdm>=4.66,<5
transformers>=4.44,<6
typeguard>=4,<5
